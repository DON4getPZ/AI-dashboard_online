# 예산 시나리오 시뮬레이션 (Budget Scenario Simulation)

## 1. 개요

예산 시나리오 시뮬레이션은 마케팅 예산 변경 시 예상되는 매출 변화를 시뮬레이션하는 기능입니다. ROAS 기반 선형 모델과 로그 체감 수익 함수를 결합하여 현실적인 예측을 제공합니다.

### 1.1 위치
- **파일**: `data/timeseries_analysis.html`
- **경로**: 성과 분석 대시보드 → 비즈니스 인사이트 → 예산 시뮬레이션

### 1.2 주요 기능
- 세그먼트별(채널/제품/브랜드/프로모션) 예산 조정 슬라이더
- 실시간 시뮬레이션 결과 계산
- 체감 수익 효과 반영 (예산 증가 시 ROAS 하락)
- 세그먼트별 투자 추천 등급 표시
- 동적 인사이트 생성

---

## 2. 모델 설계

### 2.1 하이브리드 모델 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    하이브리드 시뮬레이션 모델                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐   │
│   │ A. 세그먼트 │ → │ B. 예산배분 │ → │ C. 체감수익 │   │
│   │  별 ROAS   │    │   시뮬레이션 │    │  로그함수   │   │
│   └─────────────┘    └─────────────┘    └─────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 수식

#### A. 세그먼트별 기본 ROAS
```javascript
ROAS_segment = (전환값_segment / 비용_segment) × 100
```

#### B. 예산 변경률
```javascript
budget_change_ratio = (신규예산 - 현재예산) / 현재예산
// 예: +20% → 0.2, -10% → -0.1
```

#### C. 로그 체감 함수 적용 (핵심 수식)
```javascript
const DIMINISHING_FACTOR = 0.15;  // 체감 수익 계수

// 예산 증가 시: ROAS 하락 (체감 수익)
if (budget_change_ratio > 0) {
    adjusted_roas = current_roas × (1 - DIMINISHING_FACTOR × ln(1 + budget_change_ratio));
}

// 예산 감소 시: ROAS 상승 (효율적 타겟 집중)
else if (budget_change_ratio < 0) {
    adjusted_roas = current_roas × (1 + DIMINISHING_FACTOR × ln(1 + |budget_change_ratio|) × 0.5);
}
```

#### D. 최종 매출 계산
```javascript
predicted_revenue = new_budget × (adjusted_roas / 100)
```

---

## 3. 체감 수익 효과 (Diminishing Returns)

### 3.1 개념
예산을 늘릴수록 광고 효율(ROAS)이 점차 감소하는 현상입니다.

### 3.2 발생 원인

| 원인 | 설명 |
|------|------|
| **타겟 포화** | 핵심 타겟은 이미 노출됨, 추가 예산은 관심 낮은 유저에게 도달 |
| **광고 피로도** | 같은 유저에게 반복 노출 → 클릭률/전환율 하락 |
| **경쟁 심화** | 예산 증가 → 더 비싼 광고 지면 입찰 → CPM/CPC 상승 |
| **품질 희석** | 확장된 타겟의 구매 의도가 낮음 |

### 3.3 체감 계수 (DIMINISHING_FACTOR)

현재 설정값: `0.15`

| 예산 변경 | ROAS 변화율 | 계산 예시 (ROAS 200% 기준) |
|----------|------------|---------------------------|
| +10% | -1.4% | 200% → 197.1% |
| +20% | -2.7% | 200% → 194.5% |
| +30% | -3.9% | 200% → 192.1% |
| +50% | -6.1% | 200% → 187.8% |
| -10% | +0.8% | 200% → 201.6% |
| -30% | +1.9% | 200% → 203.8% |

---

## 4. 구현 상세

### 4.1 JavaScript 주요 함수

| 함수명 | 설명 |
|--------|------|
| `initBudgetSimulation()` | 시뮬레이션 초기화 (데이터 로드, 렌더링, 이벤트 바인딩) |
| `loadSimulationData()` | 세그먼트 데이터 로드 및 ROAS 계산 |
| `renderSimulationSliders()` | 슬라이더 UI 동적 생성 |
| `calculateAdjustedRoas()` | 로그 체감 함수 적용 ROAS 계산 |
| `updateSimulationResults()` | 시뮬레이션 결과 계산 및 UI 업데이트 |
| `updateSimulationInsight()` | 동적 인사이트 생성 |
| `bindSimulationEvents()` | 이벤트 리스너 바인딩 |

### 4.2 상태 변수

```javascript
let currentSimSegmentType = 'channel';  // 현재 선택된 세그먼트 타입
let simulationData = {};                 // 세그먼트별 원본 데이터
let budgetAdjustments = {};              // 세그먼트별 조정값 (%)
const DIMINISHING_FACTOR = 0.15;         // 체감 수익 계수
```

### 4.3 데이터 소스

- **원본 데이터**: `segmentData[segmentType]` (CSV에서 로드된 세그먼트 데이터)
- **사용 필드**:
  - `비용_예측`: 비용
  - `전환값_예측`: 매출
  - `전환수_예측`: 전환수
  - `클릭_예측`: 클릭수
  - `type`: 'actual'인 데이터만 사용

---

## 5. UI 구성

### 5.1 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│  💰 예산 시나리오 시뮬레이션                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📊 세그먼트 유형 선택                                           │
│  [채널별] [제품별] [브랜드별] [프로모션별]                         │
│                                                                 │
│  📈 세그먼트별 예산 조정                        [전체 초기화]     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 메타_전환 (ROAS 197%)           현재: 1,924만            │   │
│  │ [-30%] ════════════●══════ [+50%]  +20%                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📊 시뮬레이션 결과                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ 총 비용  │ │ 예상매출 │ │ 평균ROAS │ │ 투자효율 │         │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │
│                                                                 │
│  📋 세그먼트별 상세 결과 (테이블)                                │
│                                                                 │
│  💡 시뮬레이션 인사이트  │  ⚠️ 주의사항                          │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 슬라이더 범위
- **최소**: -30%
- **최대**: +50%
- **단계**: 5%

### 5.3 추천 등급

| 조정 ROAS | 등급 | 색상 |
|-----------|------|------|
| ≥ 150% | 증액 추천 | 녹색 (#2e7d32) |
| ≥ 100% | 유지 | 파란색 (#1565c0) |
| ≥ 50% | 효율 점검 | 주황색 (#f57c00) |
| < 50% | 감액 검토 | 빨간색 (#c62828) |

---

## 6. CSS 스타일

### 6.1 슬라이더 스타일

```css
.simulation-slider {
    -webkit-appearance: none;
    appearance: none;
    height: 6px;
    background: linear-gradient(to right,
        #c62828 0%, #c62828 37.5%,      /* 감액 영역 (빨간색) */
        #e0e0e0 37.5%, #e0e0e0 62.5%,   /* 유지 영역 (회색) */
        #2e7d32 62.5%, #2e7d32 100%     /* 증액 영역 (녹색) */
    );
    border-radius: 3px;
}

.simulation-slider::-webkit-slider-thumb {
    width: 18px;
    height: 18px;
    background: white;
    border: 2px solid var(--primary-main);
    border-radius: 50%;
    cursor: pointer;
    box-shadow: 0 2px 6px rgba(0,0,0,0.2);
}
```

---

## 7. 사용 예시

### 7.1 시나리오: 메타_전환 채널 20% 증액

**현재 상태**:
- 비용: 1,924만원
- ROAS: 197%
- 매출: 3,791만원

**시뮬레이션 결과**:
```
조정 ROAS = 197 × (1 - 0.15 × ln(1.2))
         = 197 × (1 - 0.15 × 0.182)
         = 197 × 0.973
         = 191.6%

신규 비용 = 1,924만 × 1.2 = 2,309만원
예상 매출 = 2,309만 × 1.916 = 4,424만원

추가 비용: +385만원
추가 매출: +633만원
투자 효율: 164%
```

---

## 8. 한계점 및 주의사항

### 8.1 모델 한계

| 항목 | 현재 상태 | 영향 |
|------|----------|------|
| 탄력성 계수 | 고정값 (0.15) | 세그먼트별 실제 탄력성과 다를 수 있음 |
| 과거 예산 변경 이력 | 미반영 | 실제 체감 효과 검증 불가 |
| 경쟁 환경 | 미반영 | 시장 상황에 따른 변동 미고려 |
| 계절성 | 미반영 | 성수기/비수기 효과 미고려 |

### 8.2 권장 사용 방법

1. **참고용으로 활용**: 정확한 예측이 아닌 의사결정 보조 도구
2. **소폭 조정 우선**: 급격한 예산 변경보다 단계적 조정 권장
3. **실제 결과 모니터링**: 시뮬레이션 후 실제 결과와 비교 분석
4. **복합 요소 고려**: 크리에이티브 변경, 시장 상황 등 함께 검토

---

## 9. 향후 개선 방향

1. **세그먼트별 탄력성 계수 학습**: 과거 데이터 기반 체감 계수 자동 계산
2. **계절성 반영**: Prophet 모델의 계절성 요소 통합
3. **경쟁 지수 반영**: 업종별 광고 경쟁 강도 데이터 연동
4. **A/B 테스트 연동**: 시뮬레이션 결과와 실제 실험 결과 비교 기능

---

## 10. 관련 문서

- [MULTI_PERIOD_FILTER_IMPLEMENTATION.md](./MULTI_PERIOD_FILTER_IMPLEMENTATION.md) - 기간 필터링
- [timeseries_analysis_specific.md](./timeseries_analysis_specific.md) - 시계열 분석 대시보드
- [insight_generator_guide.md](./insight_generator_guide.md) - 인사이트 생성기

---

*문서 작성일: 2025-12-17*
*마지막 수정: 2025-12-17*
